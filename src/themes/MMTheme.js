import { teal } from 'material-ui/colors';

const MMTheme = {
    palette: {
        primary: teal,
        secondary: teal,
        background: {
            appBar: '#000000',
        },
    },
};

export default MMTheme;
